<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">Welcome to Dash Board</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <div class="navbar-nav mr-auto">
        <% if user_signed_in?  %>
          <%if current_user.user_type!="admin"%>
            <div class="btn btn-outline-dark">
                <%= link_to 'User', "/user_details" , :class => "btn" %> 
            </div>
          <%elsif%>
              <div class="btn btn-outline-dark">
              <%= link_to 'User', "/admin" , :class => "btn" %> 
              </div>
          <%end%>
        <%end%>
    </div>

   
    <form class="form-inline my-2 my-lg-0">
      <%# <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search"> %>
      <%# <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button> %>
       <% if user_signed_in?  %>
        <div class="btn btn-outline-dark">
          <%= link_to 'Sign out', destroy_user_session_path , method: :delete, :class => "btn"%>
        </div>
       <% elsif %>
        <div class="btn btn-outline-dark">
          <%= link_to 'Log in', url: "/users/sign_in" , :class => "btn" %>
        </div>
        <%end%>
    </form>


  </div>

</nav>